
	  import  stdio ,  string ;

struct vec2 (int x, int y);

void sum(vec2* a,vec2* b){
	a->x += b->x;
	a->y += b->y;
}

int main(){
	vec2 a = vec2 ((1 + 3), 
		2), b;
	b.x = 4;
	sum(&a, &b);
	printf("%d\n", a.x);
}

